---
phase: 07-preview-auth-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/collab/documents/[documentId]/preview/page.tsx
  - components/collab/__tests__/preview-page.test.tsx
  - app/collab/documents/[documentId]/page.tsx
  - components/collab/__tests__/document-name.test.tsx
autonomous: true
requirements:
  - PREV-01
  - UX-01

must_haves:
  truths:
    - "User can view document blocks rendered as HTML for a specific commit SHA via /preview?commit=sha"
    - "User sees an informative message when commit query parameter is missing"
    - "User sees loading and error states while preview is being fetched"
    - "User sees the actual document name (e.g. '용역계약서_v3') in the document view header instead of generic '문서 뷰'"
  artifacts:
    - path: "app/collab/documents/[documentId]/preview/page.tsx"
      provides: "Commit-based document preview page with block rendering"
      min_lines: 50
    - path: "components/collab/__tests__/preview-page.test.tsx"
      provides: "Preview page tests covering render, missing commit, loading, error"
      min_lines: 40
    - path: "app/collab/documents/[documentId]/page.tsx"
      provides: "Document view page with dynamic document name in header"
    - path: "components/collab/__tests__/document-name.test.tsx"
      provides: "Test that document name appears in document view header"
      min_lines: 20
  key_links:
    - from: "app/collab/documents/[documentId]/preview/page.tsx"
      to: "lib/collab-api.ts"
      via: "fetchPreview(documentId, commitSha)"
      pattern: "fetchPreview"
    - from: "app/collab/documents/[documentId]/preview/page.tsx"
      to: "DOMPurify"
      via: "DOMPurify.sanitize(block.html)"
      pattern: "DOMPurify\\.sanitize"
    - from: "app/collab/documents/[documentId]/page.tsx"
      to: "stores/collab-store.ts"
      via: "useCollabStore documents + loadDocuments"
      pattern: "loadDocuments|documents\\.find"
---

<objective>
Implement the commit-based document preview page and add dynamic document names to the document view header.

Purpose: Complete the preview functionality (PREV-01) so users can view document snapshots at specific commit SHAs, and improve UX (UX-01) by showing actual document names instead of the generic "문서 뷰" text.
Output: Working preview page at /collab/documents/[documentId]/preview?commit=SHA, document view header showing real document name, tests for both features.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-preview-auth-ux-polish/07-RESEARCH.md

@lib/collab-api.ts
@stores/collab-store.ts
@app/collab/documents/[documentId]/page.tsx
@app/collab/documents/[documentId]/preview/page.tsx
@mocks/collab-handlers.ts
@mocks/collab-fixtures.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/collab-api.ts:
```typescript
export interface HtmlBlock {
  blockUuid: string;
  html: string;
  sectionPath: string;
  position: number;
}

export interface PreviewResponse {
  commitSha: string;
  blockCount: number;
  blocks: HtmlBlock[];
}

export interface DocumentListItem {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}

export async function fetchPreview(documentId: string, commitSha: string): Promise<PreviewResponse>;
export async function fetchDocuments(): Promise<DocumentListItem[]>;
```

From stores/collab-store.ts (relevant selectors):
```typescript
documents: DocumentListItem[];
loadDocuments: () => Promise<void>;
```

MSW mock handler exists at:
- `*/api/v1/collab/documents/:documentId/preview/:commitSha` -- returns { commitSha, blockCount, blocks: mockPreviewBlocks }

Mock fixtures:
- `mockPreviewBlocks` -- array of 6 HtmlBlock objects
- `mockCollabDocuments` -- array with documents including names like "용역계약서_v3"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preview page - TDD tests then implementation (PREV-01)</name>
  <files>components/collab/__tests__/preview-page.test.tsx, app/collab/documents/[documentId]/preview/page.tsx</files>
  <behavior>
    - Test 1: Renders preview blocks when commit query param is provided (fetch mockPreviewBlocks, verify block HTML rendered via sanitized innerHTML)
    - Test 2: Shows "commit 파라미터가 필요합니다" message when commit query param is missing
    - Test 3: Shows loading state while fetching preview
    - Test 4: Shows error message when fetchPreview fails
    - Test 5: Renders "back to document" link pointing to /collab/documents/{documentId}
  </behavior>
  <action>
    RED: Create `components/collab/__tests__/preview-page.test.tsx` with tests for the five behaviors above.
    - Mock `next/navigation` useParams (return { documentId: "doc-1" }) and useSearchParams (return commit param or null depending on test)
    - Mock `lib/collab-api` fetchPreview to return mockPreviewBlocks from fixtures
    - Mock DOMPurify.sanitize to pass through (or use jsdom's built-in)
    - Run tests, confirm all fail (RED)

    GREEN: Replace the stub in `app/collab/documents/[documentId]/preview/page.tsx` with full implementation:
    - "use client" directive
    - Read documentId from useParams, commitSha from useSearchParams().get("commit")
    - If no commitSha, render an informative message: "commit 파라미터가 필요합니다" with a link back to the document
    - If commitSha present, call fetchPreview(documentId, commitSha) in useEffect with loading/error local state
    - Render blocks using DOMPurify.sanitize(block.html) in dangerouslySetInnerHTML
    - Include the same `hwpx-preview` CSS styles used in the document view page (copy from document view page)
    - Include a "back to document" Link to /collab/documents/{documentId}
    - Show loading skeleton while fetching
    - Show error message on failure: "미리보기 로딩 실패"
    - Display commitSha (first 8 chars) in a header/subtitle
    - Run tests, confirm all pass (GREEN)

    Do NOT create a new Zustand store for this -- use local state with direct fetchPreview call.
    Follow the existing code pattern from the document view page for DOMPurify usage and hwpx-preview styles.
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/work/hwp-parser/frontend && npx vitest run components/collab/__tests__/preview-page.test.tsx</automated>
  </verify>
  <done>Preview page renders blocks for a given commit SHA, shows error when commit param missing, handles loading/error states, all 5 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document name in view header (UX-01)</name>
  <files>components/collab/__tests__/document-name.test.tsx, app/collab/documents/[documentId]/page.tsx</files>
  <behavior>
    - Test 1: Document view header shows actual document name (e.g. "용역계약서_v3") when documents are loaded in the store
    - Test 2: Document view header falls back to "문서 뷰" when document is not found in store
  </behavior>
  <action>
    RED: Create `components/collab/__tests__/document-name.test.tsx` with tests:
    - Mock useParams to return a known documentId
    - Mock useCollabStore to provide documents array containing a document with matching id and name
    - Verify the header renders the document name, not "문서 뷰"
    - Second test: Mock useCollabStore with empty documents array, verify fallback "문서 뷰" is shown
    - Run tests, confirm they fail (RED)

    GREEN: Modify `app/collab/documents/[documentId]/page.tsx`:
    - Add `documents` and `loadDocuments` selectors from useCollabStore (already imported)
    - Add useEffect to call loadDocuments() when documents.length === 0
    - Derive documentName: `documents.find(d => d.id === documentId)?.name ?? "문서 뷰"`
    - Replace the hardcoded `<h1>문서 뷰</h1>` (line ~197) with `<h1>{documentName}</h1>`
    - Run tests, confirm they pass (GREEN)

    Keep the change minimal -- only add the document name lookup, don't restructure other parts of the page.
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/work/hwp-parser/frontend && npx vitest run components/collab/__tests__/document-name.test.tsx</automated>
  </verify>
  <done>Document view header displays "용역계약서_v3" (or actual name) when documents are loaded, falls back to "문서 뷰" when not found, both tests pass</done>
</task>

</tasks>

<verification>
```bash
cd /Users/hipark/dev/work/hwp-parser/frontend && npx vitest run components/collab/__tests__/preview-page.test.tsx components/collab/__tests__/document-name.test.tsx
```
All new tests pass. No existing tests broken:
```bash
cd /Users/hipark/dev/work/hwp-parser/frontend && npm run test:run
```
Full suite still green (225+ tests).
</verification>

<success_criteria>
- /collab/documents/{documentId}/preview?commit=SHA renders block HTML using DOMPurify
- /collab/documents/{documentId}/preview without commit param shows informative error
- Document view page header shows actual document name from store
- 7+ new tests pass, full suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/07-preview-auth-ux-polish/07-01-SUMMARY.md`
</output>
